spec:
  template:
    spec:
      containers:
        - 
          resources:
            requests:
              cpu: "0.2"
#              memory: 256Mi
            limits:
              cpu: "1.0"
#              memory: 256Mi
          env:
          - name: USERDB_USERNAME
            value: "marco"
          - name: USERDB_PASSWORD
            value: "superman"
          - name: USERDB_DATABASE
            value: "tagsystem_user"